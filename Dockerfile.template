FROM resin/%%RESIN_MACHINE_NAME%%:latest

ENV INITSYSTEM on

WORKDIR /usr/src/app

COPY jobs.txt deps.sh package.json ./

RUN DEBIAN_FRONTEND=noninteractive JOBS=MAX npm install --unsafe-perm

RUN crontab jobs.txt

COPY /app ./

CMD [ "npm", "start" ] 
